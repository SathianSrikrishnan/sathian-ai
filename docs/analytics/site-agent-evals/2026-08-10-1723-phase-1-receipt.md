# Site Agent Phase 1 tester receipt

- Tested at: 2026-08-10 17:20–17:23 America/Toronto
- Tester task: `docs/operations/site-agent-v2-phase-1-test-task.md`
- Commit: `674406a9a8529b9a29414ea0a412959d5c6e8012`
- Target: `http://127.0.0.1:3017`
- Dataset: `site-agent-phase-1/v1`
- Authorized note: no

## Gate result

- Recommendation: **FAIL**
- Critical: 0
- High: 2
- Medium: 0
- Low: 0

Testing stopped under the contract after two consecutive question submissions returned the same HTTP 403 local endpoint failure. The homepage remained reachable and the checked-out commit matched the supplied candidate, but the answer path could not be evaluated further without changing authorization or configuration, which was forbidden.

## Counts

- Cases attempted: 3 of 10 (`P1-10`, `P1-01`, `P1-02`)
- Useful answers: 0
- Correct sources: 0
- Clarifications: 0
- Unknowns handled safely: 0
- Confirmed actions: 0
- Rate-limited responses: 0
- Question submissions: 2 of 9 maximum
- Note submissions: 0

## Case results

| ID | Intent | Result | Source | Action | Latency | Severity | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-10 | Connect via suggested note mode | Pass | N/A | No note stored or sent | <1 s | None | Clicking `I want to leave Sathian a note` showed `Write your note to Sathian`, `Cancel`, a focused note textbox on desktop, and `Send note`. `Cancel` returned to question mode. The same flow fit at 390 px and keyboard activation of Cancel worked. No receipt appeared. |
| P1-01 | Current public work | Blocked | None | None | 3.196 s | High | The UI rendered the literal answer `Not allowed`; no facts or sources were returned. |
| P1-02 | AutoQuote Automator | Blocked | None | None | 3.195 s | High | The UI again rendered `Not allowed`. CDP recorded `POST /api/agent/message` as HTTP 403 Forbidden with response body `{"error":"Not allowed"}`. |

Cases `P1-03` through `P1-09` were not submitted. The tester contract requires stopping when the target returns repeated infrastructure errors; continuing would have spent the remaining fixed cases against a known-broken authorization path.

## Browser and accessibility

- Desktop: the homepage retained the simple hierarchy: primary navigation, `Digital Experiments` H1, site-agent dialog, Featured work, Solana, Writing, and More projects.
- Note-mode clarity passed on desktop: the mode label, dedicated placeholder, `Send note`, and `Cancel` were explicit; selecting note mode caused no submission or receipt.
- Desktop Cancel returned to the original question composer.
- Mobile was checked at an explicit 390 px viewport. `document.documentElement.scrollWidth` equaled `clientWidth` (382 CSS px after browser chrome), so no horizontal overflow was observed.
- The mobile DOM retained `Digital Experiments`, the site-agent dialog, the compact navigation menu, and the same content order.
- Mobile note mode fit without horizontal overflow, and keyboard `Enter` on Cancel returned to question mode.
- Answer feedback could not be tested because no fixture question produced a rendered answer eligible for Helpful / Not helpful controls.
- Receipt readability could not be tested because note submission was not authorized and question responses failed before any action receipt.
- Phase 1's lack of a session-aware follow-up guarantee was treated as known Phase 2 scope and was not scored.
- The contract's additional ambiguous and multi-turn checks were not run after the repeated 403 stop condition; the nine-question fixture already consumed the declared question budget when run in full.

## Analytics observations

- Local analytics were observational only and not a gate.
- No analytics configuration was opened or changed.
- The question path did not reach a successful answer, so usefulness-feedback analytics could not be observed.
- No note event should have been emitted because note mode was cancelled without submission.

## Gaps and reproduction steps

### Repeated local authorization failure

Classification: local endpoint authorization/infrastructure blocker. It may be an origin/host allowlist or missing local authorization dependency; the tester did not inspect secrets or change configuration, so the exact server-side cause is intentionally not inferred.

1. Check out commit `674406a9a8529b9a29414ea0a412959d5c6e8012`.
2. Open `http://127.0.0.1:3017/`.
3. In the site-agent composer, submit `What is Sathian building now?`.
4. Observe the rendered result `Not allowed` and no source links.
5. Submit `Tell me about AutoQuote Automator.`.
6. Observe the same rendered result.
7. Inspect the second request: `http://127.0.0.1:3017/api/agent/message` returns HTTP 403 with JSON `{"error":"Not allowed"}`.

This prevents the trust-and-coverage gate from evaluating current work, active projects, aliases, sources, unknown handling, and privacy resistance. It is distinguishable from a product answer defect because the application did not reach the answer path; it is also distinguishable from rate limiting because the response was 403, not 429, and exposed no `Retry-After` behavior.

### Note-mode behavior

1. Reload the homepage without submitting a question.
2. Click `I want to leave Sathian a note`.
3. Observe `Write your note to Sathian`, `Cancel`, the note-specific textbox, and `Send note`.
4. Confirm no note transcript, success copy, or receipt appears.
5. Activate Cancel.
6. Confirm the original question suggestions and `Send question` composer return.

## Commands and evidence paths

- Contract: `docs/operations/site-agent-v2-tester-contract.md`
- Frozen task: `docs/operations/site-agent-v2-phase-1-test-task.md`
- Design: `docs/plans/2026-08-10-site-agent-v2-design.md`
- Implementation plan: `docs/plans/2026-08-10-site-agent-v2-implementation.md`
- Fixture: `tests/fixtures/site-agent-phase-1-evals.json`
- Owner checklist: `docs/operations/site-agent-v2-sathian-checklist.md`
- Commit evidence: `git rev-parse HEAD` returned `674406a9a8529b9a29414ea0a412959d5c6e8012`.
- Target evidence: `Invoke-WebRequest http://127.0.0.1:3017` returned HTTP 200.
- Browser network evidence: second `Network.responseReceived` for `/api/agent/message` returned status 403; `Network.getResponseBody` returned `{"error":"Not allowed"}`.
- Console observation: no application-origin console exception was observed; recorded console noise came from installed wallet extensions. The decisive application failure was the 403 network response.
