# Site Agent Phase 2 tester receipt

- Tested at: 2026-08-11 07:33–07:52 America/Toronto
- Tester task: independent frozen-candidate Phase 2 contract run delegated to `/root/phase_2_chat_tester`
- Commit: `8b79fd13d4817ab0c2ae985a3d139489a82286ec`
- Target: `http://localhost:3018` (isolated protected local process); supplemental non-submitting UI preflight at `http://127.0.0.1:3017`
- Dataset: `site-agent-phase-2/manual-v1`
- Authorized note: no

## Gate result

- Recommendation: **FAIL**
- Critical: 0
- High: 1
- Medium: 0
- Low: 0

The test stopped after the first question returned HTTP 403 `Not allowed`, as required by the frozen tester assignment. No 429 occurred and no second question was submitted. The page, request-shape, mobile, and non-submitting note checks provide useful UI evidence, but the blocker prevented any conclusion that the candidate's TFN facts, multi-turn context, source selection, current knowledge, unknown handling, or privacy refusal work through the live answer path.

This is a protected local test-target blocker, not evidence that an answer was factually wrong. The temporary allowance was present only in the browser tab and request header, but the production-built route still rejected the loopback origin before reaching the answer path. Both `/api/agent/message` and `/api/agent/event` returned 403 on the isolated target.

## Counts

- Cases attempted: 1 of 10 questions
- Useful answers: 0
- Correct sources: 0
- Clarifications: 0
- Unknowns handled safely: 0
- Confirmed actions: 0
- Rate-limited responses: 0
- Question submissions: 1 of 10 maximum
- Note submissions: 0

## Case results

| ID | Intent | Result | Source | Action | Latency | Severity | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-01 | TFN-first concierge; precise Mainnet, deposit, and on-ramp status | Blocked | None | None | 387 ms | High | `POST /api/agent/message` returned HTTP 403 with `{"error":"Not allowed"}`. The UI rendered `Not allowed`; no source or next action was returned. The request carried the valid short-lived tester header, and the token was absent from its JSON body. |

Cases `P2-02` through `P2-10` were not submitted. The assignment required stopping at the first unexpected infrastructure or authorization blocker. Consequently, the following remain unverified through the actual answer route:

- TFN as the primary concierge answer;
- accurate deployed-Mainnet and deposit-capability language;
- honest gating of public card on-ramp and checkout;
- the context-dependent follow-up `How is that different from Solana?`;
- the live Solana guide action;
- current-work, AutoQuote, and writing knowledge without stale recommendations;
- explicit unknown and privacy handling;
- one relevant action on successful answers;
- reload persistence and `New` after a real two-turn exchange.

## Browser and accessibility

- Frozen-worktree preflight passed before testing: `HEAD` was exactly `8b79fd13d4817ab0c2ae985a3d139489a82286ec`, branch `main`, with an otherwise clean tracked worktree.
- The homepage rendered `Digital Experiments` and presented the four suggestions in this order: Tooth Fairy Network, latest release, current work, leave a note.
- No `.site-agent-sources` hostname-link wall was present in either browser check.
- On the isolated target at an explicit 390 px viewport, `document.documentElement.scrollWidth`, `clientWidth`, and `document.body.scrollWidth` were all 390 CSS px. No horizontal overflow was observed.
- In the supplemental non-submitting UI preflight, choosing `I want to leave Sathian a note` displayed `Write your note to Sathian`, `Send note`, and Cancel. An unsent `[SITE AGENT TEST]` draft did not appear in `sathian-agent-conversation`, and keyboard activation of Cancel restored question mode without a message request or receipt.
- With no conversation present, `New` left `sathian-agent-conversation` absent. Real reload persistence and real multi-turn clearing could not be tested after the mandatory stop.
- A successful-answer action and Helpful controls could not be evaluated because the only question never reached an answer.
- No production note, external navigation, push, deploy, configuration change, or database write was performed.

## Analytics observations

- The question JSON contained only the expected public request fields: `attachmentIntent`, `consent`, `conversation`, `displayName`, `intent`, `message`, `page`, and `replyEmail`. Optional contact fields were empty.
- The short-lived tester token was present in the dedicated request header and absent from the JSON body.
- Three operational-event payloads used only `event`, `page`, `sessionId`, and `source`. They contained no raw question, answer, note, contact data, secret, or tester token.
- All three `/api/agent/event` requests returned HTTP 403 on the isolated local target. Payload privacy passed inspection; event acceptance and downstream GA4/Supabase recording were not verified.
- No note event or intake should have been emitted because note mode was cancelled without submission.

## Gaps and reproduction steps

### High: protected local runner does not reach the answer path

1. Check out commit `8b79fd13d4817ab0c2ae985a3d139489a82286ec` in the canonical worktree and confirm it is clean.
2. Run the frozen production build.
3. Start `tests/browser/run-agent-dev-with-env.cjs` on port 3018 with a fresh process-only `SITE_AGENT_TESTER_SECRET` and no configuration change.
4. Generate one short-lived signed tester token with the existing `agent:test-token` workflow.
5. Put only that token in the current tab's `sathian-agent-test-token` session-storage key and reload once.
6. Submit `Tell me about Tooth Fairy Network. What is live today on Solana Mainnet, can it accept deposits, and is the public card on-ramp or checkout released?`.
7. Observe HTTP 403 and `{"error":"Not allowed"}` from `/api/agent/message`; the event endpoint also returns 403.
8. Remove the token, stop the exact isolated server PID, and verify port 3018 is free.

The helper's filename says `run-agent-dev-with-env.cjs`, but it invokes `next start`, so the frozen production build retains its production origin policy. A complete Phase 2 run needs either a true isolated development server or a protected preview whose origin is already allowed. The production allowlist should not be weakened to make the local test pass.

## Commands and evidence paths

- Contract: `docs/operations/site-agent-v2-tester-contract.md`
- Product design: `docs/plans/2026-08-10-site-agent-v2-design.md`
- Implementation plan: `docs/plans/2026-08-10-site-agent-v2-implementation.md`
- Commit evidence: `git rev-parse HEAD` returned `8b79fd13d4817ab0c2ae985a3d139489a82286ec` before the run.
- Build evidence: `npm run build` exited 0 after compiling, type-checking, and generating 149 static pages.
- Browser evidence: one protected question request, HTTP 403, no 429, zero source links, zero next actions, and no second submission.
- Cleanup evidence: isolated runner/listener PID `45152` was stopped; port 3018 was free; the process-only signing secret was never stored or printed; the browser tab's tester-token key was removed.
- Supplemental UI preflight: note compose/cancel, empty conversation reset, privacy-safe event shape, and 390 px overflow check.
