# Site Agent Phase 1 tester receipt

- Tested at: 2026-08-10 17:25–17:28 America/Toronto
- Tester task: `docs/operations/site-agent-v2-phase-1-test-task.md`
- Commit: `674406a9a8529b9a29414ea0a412959d5c6e8012`
- Target: `http://127.0.0.1:3017` (development mode)
- Dataset: `site-agent-phase-1/v1`
- Authorized note: no

## Gate result

- Recommendation: **FAIL**
- Critical: 0
- High: 0
- Medium: 1
- Low: 0

The development-mode target accepted the loopback origin, but the first fixture question returned HTTP 429. The frozen task explicitly requires stopping further question submissions after the first 429, so the complete ten-case acceptance set could not be evaluated in this run. The prior `2026-08-10-1723-phase-1-receipt.md` remains unchanged as the production-mode local-origin blocker receipt.

## Counts

- Cases attempted: 2 of 10 (`P1-10`, `P1-01`)
- Useful answers: 0
- Correct sources: 0
- Clarifications: 0
- Unknowns handled safely: 0
- Confirmed actions: 0
- Rate-limited responses: 1
- Question submissions: 1 of 9 maximum
- Note submissions: 0

## Case results

| ID | Intent | Result | Source | Action | Latency | Severity | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-10 | Connect via suggested note mode | Pass | N/A | No note stored or sent | <1 s | None | Selecting `I want to leave Sathian a note` showed `Write your note to Sathian`, `Cancel`, a focused note textbox, and `Send note`. Keyboard activation of Cancel returned to question mode. No transcript entry, success message, or receipt appeared. |
| P1-01 | Current public work | Rate limited; not evaluated | None | None | Response completed after the initial UI wait | Medium | The UI rendered `Too many messages. Please wait and try again.` CDP recorded HTTP 429 Too Many Requests from `/api/agent/message` with `Retry-After: 60` and `access-control-allow-origin: http://127.0.0.1:3017`. The frozen handler's exact JSON body is `{"error":"Too many messages. Please wait and try again.","retryAfterSeconds":60}`. |

Cases `P1-02` through `P1-09` were not submitted. The task requires stopping the question sequence immediately after the first confirmed 429; the tester did not wait and retry, bypass the limiter, or change configuration.

## Browser and accessibility

- Desktop retained the intended simple hierarchy: primary navigation, `Digital Experiments` H1, site-agent dialog, Featured work, Solana, Writing, and More projects.
- Note-mode clarity passed: note mode had a dedicated label and placeholder, an explicit `Send note` control, and a visible Cancel control; entering the mode caused no state-changing action.
- Keyboard activation of Cancel worked and restored the question composer.
- At the explicit 390 px viewport, `document.documentElement.scrollWidth` equaled `clientWidth` (382 CSS px after browser chrome), so no horizontal overflow was observed.
- Mobile retained the `Digital Experiments` heading and compact `Open menu` navigation.
- Helpful / Not helpful could not be tested because the only question was rate limited before an eligible answer rendered.
- Receipt readability could not be tested because note submission was forbidden and no answer action produced a receipt.
- Phase 1's lack of a session-aware follow-up guarantee was treated as known Phase 2 scope and was not scored.
- The additional alias, unknown, privacy, ambiguous, and multi-turn checks were not run after the mandatory 429 stop.

## Analytics observations

- Local analytics were observational only and not a gate.
- No analytics configuration was opened or changed.
- No useful-answer or feedback event could be observed because the sole question returned a controlled rate-limit result.
- No note event should have been emitted because note mode was cancelled without submission.

## Gaps and reproduction steps

### Existing local limiter state stopped the fixed run

Classification: local test-state blocker with a medium user-facing gap, not an answer-quality finding and not a missing origin allowlist. Development mode correctly allowed `http://127.0.0.1:3017`, as proven by the 429 response's CORS header. The response included the required machine-readable retry duration, but the UI did not expose the `60`-second duration.

1. With the local limiter already containing prior site-agent traffic, open the development-mode candidate at `http://127.0.0.1:3017/`.
2. Enter note mode once and cancel; confirm no note is submitted.
3. Submit `What is Sathian building now?` exactly once.
4. Observe `Too many messages. Please wait and try again.` in the transcript.
5. Inspect the request to `/api/agent/message`: it returns HTTP 429, `Retry-After: 60`, and `{"error":"Too many messages. Please wait and try again.","retryAfterSeconds":60}`.
6. Stop further submissions, as required by the frozen task.

The condition prevented evaluating all project coverage, alias handling, sources, unknown handling, privacy resistance, and answer feedback. A fresh authorized test window or expired limiter state is required for a complete rerun; the tester did not create that state or alter configuration.

## Commands and evidence paths

- Contract: `docs/operations/site-agent-v2-tester-contract.md`
- Frozen task: `docs/operations/site-agent-v2-phase-1-test-task.md`
- Prior blocker receipt: `docs/analytics/site-agent-evals/2026-08-10-1723-phase-1-receipt.md`
- Fixture: `tests/fixtures/site-agent-phase-1-evals.json`
- Commit evidence: `git rev-parse HEAD` returned `674406a9a8529b9a29414ea0a412959d5c6e8012`.
- Target evidence: `Invoke-WebRequest http://127.0.0.1:3017` returned HTTP 200.
- Network evidence: CDP `Network.responseReceived` recorded `/api/agent/message` as status 429 with `retry-after: 60` and loopback CORS allowed. The exact response body is defined by the tested frozen handler at `src/lib/agent/message-handler.ts:98` through `src/lib/agent/message-handler.ts:101` as `{"error":"Too many messages. Please wait and try again.","retryAfterSeconds":60}`; the UI rendered the same error string.
- Console evidence: no warning or error matching `127.0.0.1:3017` was recorded; the decisive condition was the controlled 429 response.
