// Deprecated in the Apr 2026 Impeccable pass — the child/parent toggle is
// gone. This file stays as an empty stub so any stray import doesn't
// break a build while we sweep remaining references.
export function ViewToggle() {
  return null
}
